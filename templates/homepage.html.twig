{% extends 'base.html.twig' %}

{% block body %}
  <nav>
    <header>
      <h2>RP</h2>
    </header>
    <div>
      {% for place in places %}
        <a href="{{ path('place.index', { slug: place.slug }) }}">
          <h3>{{ place.title }}</h3>
          <p>{{ place.description }}</p>
        </a>
      {% endfor %}
    </div>
  </nav>

  <nav>
    <header>
      <h2>RP</h2>
    </header>
    <div>
      {% for category in categories %}
        <a href="#{{ category.slug }}">
          <h3>{{ category.title }}</h3>
          <p>{{ category.description }}</p>
        </a>
      {% endfor %}
    </div>
  </nav>
{% endblock %}
